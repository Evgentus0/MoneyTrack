<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MoneyTrack.Clients.Mobile.Views.AddNewTransactionPage"
             Title="{Binding Title}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Entry Grid.Row="0" Placeholder="How much" Text="{Binding Path=NewTransaction.Quantity}"/>
        <Entry Grid.Row="1" Placeholder="Description" Text="{Binding Path=NewTransaction.Description}"/>
        <Picker Grid.Row="2" ItemsSource="{Binding Path=Categories}" SelectedItem="{Binding Path=NewTransaction.Category}"/>
        <Picker Grid.Row="3" ItemsSource="{Binding Path=Accounts}" SelectedItem="{Binding Path=NewTransaction.Account}"/>
    </Grid>
</ContentPage>